{
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspaces run --if-present build",
    "test": "npm --workspaces run --if-present test"
  },
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.1.3",
    "typescript": "^4.4.4"
  }
}
